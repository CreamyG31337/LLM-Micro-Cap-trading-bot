{
  "name": "web-dashboard",
  "version": "1.0.0",
  "description": "TypeScript build configuration for web dashboard",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf static/js/*.js static/js/*.js.map"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "private": true
}
